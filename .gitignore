__pycache__

# Atoma runtime artifacts
atoma_logs.txt
atoma_output.txt
atoma_ops.log
events.json
session.json
logs/

# Dependencies
node_modules/
dist/**/node_modules/
dist/**/bun.lock*

# Atoma source checkout created by the runner when atoma_version=source.
# A bare `git add -A` otherwise commits it as a dangling gitlink (no .gitmodules).
atoma-src/
