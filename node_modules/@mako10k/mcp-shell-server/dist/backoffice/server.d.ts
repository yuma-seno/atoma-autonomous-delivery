import { ProcessManager } from '../core/process-manager.js';
import { TerminalManager } from '../core/terminal-manager.js';
import { FileManager } from '../core/file-manager.js';
import { CommandHistoryManager } from '../core/enhanced-history-manager.js';
interface BackofficeDeps {
    processManager: ProcessManager;
    terminalManager: TerminalManager;
    fileManager: FileManager;
    historyManager: CommandHistoryManager;
}
export declare class BackofficeServer {
    private deps;
    private server;
    private readonly host;
    private readonly port;
    constructor(deps: BackofficeDeps, port?: number);
    start(): Promise<void>;
    stop(): Promise<void>;
    getListenPort(): number;
    private handleHistoryList;
    private handleHistoryGet;
    private handleExecutionsList;
    private handleExecutionGet;
    private handleExecutionOutputs;
    private handleTerminalsList;
    private handleTerminalGet;
    private handleTerminalOutput;
    private isRemoteBackend;
    private getRemoteService;
    private ensureRemoteBackend;
    private handleRemoteRequest;
    private handleDashboard;
    private handleRemoteExecGet;
    private handleRemoteExecOutputs;
    private handleRemoteExecKill;
    private initSSE;
    private writeSSE;
    private handleTerminalSSE;
    private handleRemoteExecSSE;
    private serveStatic;
    private json;
    private isLocalAddress;
}
export {};
//# sourceMappingURL=server.d.ts.map